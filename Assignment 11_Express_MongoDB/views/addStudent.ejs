<%- include('partials/header') %>

<div class="row justify-content-center">
  <div class="col-lg-8 col-md-10">
    
    <div class="d-flex align-items-center mb-4">
      <a href="/students" class="btn btn-sm btn-light border me-3 rounded-3 px-3 py-2 text-secondary">
        <i class="bi bi-arrow-left"></i> Back to Directory
      </a>
      <h2 class="m-0 font-weight-700">Add New Student</h2>
    </div>

    <!-- Error Alerts -->
    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <!-- Add Form Card -->
    <div class="dashboard-card">
      <form action="/students/add" method="POST" class="needs-validation" novalidate>
        
        <div class="row g-3">
          <!-- Student Name -->
          <div class="col-md-6">
            <label for="name" class="form-label">Full Name <span class="text-danger">*</span></label>
            <input 
              type="text" 
              class="form-control" 
              id="name" 
              name="name" 
              value="<%= formData.name || '' %>" 
              required 
              placeholder="e.g. Swayam Gupta"
            >
            <div class="invalid-feedback">Please enter student name.</div>
          </div>

          <!-- Roll Number -->
          <div class="col-md-6">
            <label for="rollno" class="form-label">Roll Number <span class="text-danger">*</span></label>
            <input 
              type="number" 
              class="form-control" 
              id="rollno" 
              name="rollno" 
              value="<%= formData.rollno || '' %>" 
              required 
              placeholder="e.g. 101"
            >
            <div class="invalid-feedback">Please enter unique roll number.</div>
          </div>

          <!-- Course -->
          <div class="col-md-6">
            <label for="course" class="form-label">Course / Program <span class="text-danger">*</span></label>
            <select class="form-select" id="course" name="course" required>
              <option value="" disabled <%= !formData.course ? 'selected' : '' %>>--Select course--</option>
              <option value="Computer Science" <%= formData.course === 'Computer Science' ? 'selected' : '' %>>Computer Science</option>
              <option value="BCA" <%= formData.course === 'BCA' ? 'selected' : '' %>>BCA</option>
              <option value="Computer Science with IT" <%= formData.course === 'Computer Science with IT' ? 'selected' : '' %>>Computer Science with IT</option>
            </select>
            <div class="invalid-feedback">Please specify the course.</div>
          </div>

          <!-- Age -->
          <div class="col-md-6">
            <label for="age" class="form-label">Age <span class="text-danger">*</span></label>
            <input 
              type="number" 
              class="form-control" 
              id="age" 
              name="age" 
              value="<%= formData.age || '' %>" 
              min="16" 
              required 
              placeholder="Min age: 16"
            >
            <div class="form-text text-muted">Student age must be greater than 15.</div>
            <div class="invalid-feedback">Age must be at least 16.</div>
          </div>

          <!-- Email -->
          <div class="col-md-6">
            <label for="email" class="form-label">Email Address <span class="text-danger">*</span></label>
            <div class="input-group">
              <span class="input-group-text bg-light text-muted border-end-0" style="border-radius: 12px 0 0 12px;"><i class="bi bi-envelope"></i></span>
              <input 
                type="email" 
                class="form-control border-start-0" 
                id="email" 
                name="email" 
                value="<%= formData.email || '' %>" 
                required 
                placeholder="e.g. Swayamgupta@gmail.com"
                style="border-radius: 0 12px 12px 0;"
              >
              <div class="invalid-feedback">Please enter a valid email address.</div>
            </div>
          </div>

          <!-- City -->
          <div class="col-md-6">
            <label for="city" class="form-label">City <span class="text-danger">*</span></label>
            <input 
              type="text" 
              class="form-control" 
              id="city" 
              name="city" 
              value="<%= formData.city || '' %>" 
              required 
              placeholder="e.g. Kanpur"
            >
            <div class="invalid-feedback">Please specify the city.</div>
          </div>
        </div>

        <hr class="my-4 text-muted">

        <div class="d-flex justify-content-end gap-2">
          <button type="button" class="btn btn-light border px-4 rounded-3 text-secondary" onclick="this.closest('form').reset(); Array.from(this.closest('form').querySelectorAll('input')).forEach(i => { i.setAttribute('value', ''); i.value = ''; }); this.closest('form').classList.remove('was-validated');">
            Reset
          </button>
          <button type="submit" class="btn btn-primary-gradient px-4">
            <i class="bi bi-check-circle me-1"></i> Save Student
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
// Bootstrap custom validation trigger
(() => {
  'use strict'
  const forms = document.querySelectorAll('.needs-validation')
  Array.from(forms).forEach(form => {
    form.addEventListener('submit', event => {
      if (!form.checkValidity()) {
        event.preventDefault()
        event.stopPropagation()
      }
      form.classList.add('was-validated')
    }, false)
  })
})()
</script>

<%- include('partials/footer') %>
